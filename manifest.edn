{:actor/id        "karakuri"
 :actor/glyph     "繰"
 :actor/tier      :tier-b
 :actor/domain    "karakuri.etzhayyim.com"
 :actor/did       "did:web:etzhayyim.com:actor:karakuri"
 :actor/purpose   "Web-service-to-CLI (絡繰 = the mechanism that drives a manual service by command; the karakuri-kaizen sense — clever automation that removes manual toil). Gives a member a uniform CLI / command vocabulary (ServiceOp) over GUI-only SaaS (Squarespace / Wix / Notion / Shopify / …), driving the MEMBER's OWN account through the safest adapter (T1 official-API > T2 ToS-permitted headless-browser > T3 structured export), logging every op to the kotoba Datom log. The charter-clean inverse of clianything.org: own-account-only, official-API-first, ToS-honest (no detection-evasion), no-server-key, member-signed mutate, data-portability over lock-in."
 :actor/status    :r0
 :actor/adr       "2606039200"
 :actor/related   ["2606012100" "2605231525" "2605215000" "2605181100" "2605192200" "2606038400" "2606033600" "2605302130" "2605312030" "2606013800"]
 :actor/labor
 {:isic   "J6201"
  :isco   ["2512" "3514"]
  :unspsc ["81112"]
  :lps-rank nil}

 :actor/charter-rider {:rider/version "2.0" :rider/applied true}

 :actor/adapter-tiers
 [{:tier/id "T1" :tier/kind :official-api  :tier/preference 1 :tier/note "preferred — service's published API; ToS-sanctioned, rate-limit-documented"}
  {:tier/id "T2" :tier/kind :headless-browser :tier/engine :browser-use :tier/preference 2 :tier/note "browser-use (LangGraph-driven browser agent over Playwright) over the member's OWN GUI session; ONLY where the service browser-automation stance permits (G2); no detection-evasion — proxy/captcha/stealth verbs are structurally unrepresentable (src/karakuri/methods/t2_browser.cljc). Google/Facebook are :api-ok but browser-prohibited → routed to T1, T2 refused"}
  {:tier/id "T3" :tier/kind :structured-export :tier/preference 3 :tier/note "import/export round-trip; the data-portability / anti-lock-in leg"}]

 :actor/gates
 [{:gate/id "G1" :gate/name "member-principal-own-account-only" :gate/req "drives ONLY accounts the member owns + has authenticated; no third-party-account access; no harvesting others' data; no scrape-this-site product (himotoki own-data-only prior art)"}
  {:gate/id "G2" :gate/name "official-api-preferred-tos-honest" :gate/req "prefer T1 official API; T2 headless-browser ONLY where the service ToS permits automation; NO detection-evasion (no captcha-farm, no proxy/IP cloaking, no rate-limit circumvention); robots/rate-limit/backoff respected; :automation-prohibited service refuses T2 by construction"}
  {:gate/id "G3" :gate/name "no-server-key" :gate/req "member service credentials/sessions held by member-operator, encrypted (com.etzhayyim.encrypted.*, ADR-2605181100), never platform-held; mutating op authorized by member signature; server signature refused (ADR-2605231525)"}
  {:gate/id "G4" :gate/name "murakumo-only" :gate/req "NL->command planning via LiteLLM 127.0.0.1:4000 only (ADR-2605215000)"}
  {:gate/id "G5" :gate/name "read-default-mutate-gated" :gate/req ":read + :export ship at R0; :create/:update/:delete (esp. destructive :delete) require member-sig + explicit dry-run confirm; live execution Council Lv6+ + operator gated"}
  {:gate/id "G6" :gate/name "outward-gated" :gate/req "ANY live network call to a third-party service (every T1/T2/T3 adapter execution) Council Lv6+ + operator gated; R0 = offline parse/plan/dry-run only"}
  {:gate/id "G7" :gate/name "kotoba-eavt-audit" :gate/req "every planned + executed ServiceOp written to the kotoba Datom log (as-of, replayable); member can audit exactly what touched their account"}
  {:gate/id "G8" :gate/name "sourcing-honesty" :gate/req ":representative service registry; bounded subset; unknown service degrades honestly (:unknown-service), never guesses"}
  {:gate/id "G9" :gate/name "pii-portability-consent" :gate/req "exported data is the member's OWN data only, consent-bound + encrypted (com.etzhayyim.encrypted.*); no third-party PII collection"}]

 :actor/non-goals
 [{:ng/id "N1" :ng/req "not a scraper / surveillance / third-party-data-harvesting tool"}
  {:ng/id "N2" :ng/req "no detection-evasion / anti-bot circumvention / captcha-farming / proxy-cloaking"}
  {:ng/id "N3" :ng/req "no credential-stuffing / account-takeover / shared-or-borrowed-account abuse"}
  {:ng/id "N4" :ng/req "no paywall / license / DRM circumvention or content piracy (portability = member's OWN data only)"}
  {:ng/id "N5" :ng/req "not a bot-farm / mass-automation / spam / fake-engagement engine"}
  {:ng/id "N6" :ng/req "no driving of prohibited-content or third-party ad/affiliate systems (Charter-Rider §2(a)-(h))"}]

 :actor/cells
 [{:cell/id "service_resolve"  :cell/kind :langgraph :cell/runtime :wasm :cell/node "reuben"}
  {:cell/id "command_plan"     :cell/kind :langgraph :cell/runtime :wasm :cell/node "simeon"}
  {:cell/id "session_broker"   :cell/kind :langgraph :cell/runtime :wasm :cell/node "levi"}
  {:cell/id "adapter_invoke"   :cell/kind :langgraph :cell/runtime :wasm :cell/node "judah"}
  {:cell/id "export_roundtrip" :cell/kind :langgraph :cell/runtime :wasm :cell/node "zebulun"}]

 :actor/lex
 [{:lex/id "serviceCapabilityRecord"}
  {:lex/id "commandPlan"}
  {:lex/id "serviceOp"}
  {:lex/id "sessionGrant"}
  {:lex/id "exportArtifact"}]}
